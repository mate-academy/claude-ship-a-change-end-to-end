---
description: Scaffold endpoint users
argument-hint: <type> <name>
---
# Set Parameters
Arguments:
- $1 - user type
- $2 - username
If `$1` or `$2` is missing, ask for it rather than guessing a path

# Print Output
- print in the console Result by concatenating $1 and $2